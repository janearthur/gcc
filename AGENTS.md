# Agent notes for this GCC fork

Cursor Cloud build and test instructions live in `.cursor/AGENTS.md`.

Use `./.cursor/build-gcc.sh` for an out-of-tree, non-bootstrap C/C++ build on the Cloud VM.
